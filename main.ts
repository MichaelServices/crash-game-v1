player.onChat("Crash = True", function () {
    for (let index = 0; index < 99999; index++) {
        mobs.kill(
        mobs.target(ALL_PLAYERS)
        )
        gameplay.timeSet(gameplay.time(DAY))
        gameplay.timeSet(gameplay.time(NIGHT))
        gameplay.title(mobs.target(ALL_PLAYERS), "MICHAEL SERVICES", "GET CRASHED BY MICHAEL SERVICES NO MORE GAME EZ")
    }
})
player.say("The mod is on and enabled type Crash = True to start crash (Everyone can see this msg lol)")
